# VaakSetu Frontend — React + Vite
FROM node:20-slim

WORKDIR /app

# Install dependencies
COPY frontend/package*.json ./
RUN npm install --legacy-peer-deps

# Copy source
COPY frontend/ .

EXPOSE 5173

# Run Vite dev server (proxied to backend)
CMD ["npx", "vite", "--host", "0.0.0.0"]
